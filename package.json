{
  "name": "uplinkr",
  "version": "1.0.0-release",
  "description": "Modern file uploader and CDN service",
  "main": "server/index.js",
  "scripts": {
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "server": "nodemon server/index.js",
    "install:all": "npm install && cd client && npm install && cd ..",
    "client": "cd client && npm run dev",
    "build": "cd client && npm run build",
    "start": "node server/index.js"
  },
  "keywords": ["uploader", "cdn", "file-sharing"],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "express": "^4.18.2",
    "multer": "^1.4.5-lts.1",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "nanoid": "^3.3.7",
    "helmet": "^7.1.0",
    "compression": "^1.7.4",
    "node-cron": "^3.0.3",
    "express-rate-limit": "^7.1.5",
    "geoip-lite": "^1.4.10"
  },
  "devDependencies": {
    "nodemon": "^3.0.1",
    "concurrently": "^8.2.2"
  }
}
